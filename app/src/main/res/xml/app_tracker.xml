<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="TypographyDashes">


    <!-- The apps Analytics Tracking Id -->
    <string name="ga_trackingId">UA-54690253-1</string>

    <!-- Percentage of events to include in reports -->
    <string name="ga_sampleFrequency">100.0</string>

    <!-- Enable automatic Activity measurement -->
    <bool name="ga_autoActivityTracking">true</bool>

    <!-- catch and report uncaught exceptions from the app -->
    <bool name="ga_reportUncaughtExceptions">
        true
    </bool>

    <!-- How long a session exists before giving up -->
    <integer name="ga_sessionTimeout">-1
    </integer>

    <!-- If ga_autoActivityTracking is enabled, an alternate screen name can be specified to
    substitute for the full length canonical Activity name in screen view hit. In order to
    specify an alternate screen name use an <screenName> element, with the name attribute
    specifying the canonical name, and the value the alias to use instead. -->
    <screenName name="com.twistedsin.app.lcsmashup.activities.ActivityDashboard">
        Activity Dashboard
    </screenName>

    <screenName name="com.twistedsin.app.lcsmashup.activities.ActivityGame">
        Activity Game
    </screenName>

    <screenName name="com.twistedsin.app.lcsmashup.activities.ActivityAbout">
        Activity About
    </screenName>

    <screenName name="com.twistedsin.app.lcsmashup.activities.ActivityLiveStream">
        Activity Live Stream
    </screenName>

    <screenName name="com.twistedsin.app.lcsmashup.activities.ActivityLiveFeed">
        Activity Live Feed
    </screenName>

    <screenName name="com.twistedsin.app.lcsmashup.activities.ActivitySchedule">
        Activity Schedule
    </screenName>

    <screenName name="com.twistedsin.app.lcsmashup.activities.ActivityNewsDetail">
        Activity News Detail
    </screenName>

    <screenName name="com.twistedsin.app.lcsmashup.activities.ActivityNewsVideo">
        Activity News Video
    </screenName>

</resources>